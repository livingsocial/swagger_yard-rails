require 'spec_helper'

describe SwaggerYard::Rails do
  it 'has a version number' do
    expect(SwaggerYard::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
